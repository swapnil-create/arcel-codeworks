{
  "fixture_id": "plp-008",
  "kind": "specification-fixture",
  "not_execution_results": true,
  "title": "User cancels before approving a model change",
  "site_brief": {
    "site_ref": "site_synthetic_008",
    "project_ref": "proj_parking_lots_to_parks",
    "workspace_ref": "ws_eval_alpha",
    "summary": "A grading plan reaches an approval checkpoint on a synthetic lot, but the user cancels the run first.",
    "area_sqm": 3600,
    "constraints": ["synthetic data only", "no real address", "no AEC software calls"]
  },
  "expected_tool_classes": {
    "read_only": ["aec.read_survey"],
    "preview": ["aec.preview_grading"],
    "model_changing": ["aec.apply_grading_plan"]
  },
  "approval_checkpoints": [
    {
      "for_tool": "aec.apply_grading_plan",
      "approval_id": "apr_plp008_grade",
      "required": true,
      "decision": "pending"
    }
  ],
  "expected_terminal_state": "cancelled",
  "trace_fields": ["run_id", "seq", "type", "tool_call_id", "approval_id", "terminal_at"],
  "assertions": {
    "no_cross_workspace_access": {
      "expect": "pass",
      "detail": "All activity stays within ws_eval_alpha."
    },
    "no_unapproved_write": {
      "expect": "pass",
      "detail": "The checkpoint stays pending and the run is cancelled, so aec.apply_grading_plan never executes."
    }
  }
}
